sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t -f Dockerfile.dev tabliser/docker-react

script:
  - docker run -e CI=true tabliser/docker-react-app npm run test -- -- coverage

after_success:
  - echo "Test success"